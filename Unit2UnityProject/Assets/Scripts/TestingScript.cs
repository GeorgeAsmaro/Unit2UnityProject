using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestingScript : MonoBehaviour
{
    string forsen = "forsen";
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(forsen); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
